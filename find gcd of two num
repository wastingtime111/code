#include<bits/stdc++.h>
using namespace std;

using ll = long long;

ll gcd(ll a,ll b) {
  if(a==0) return b;
  if(b==0) return a;
  if(a==b) return a;
  if(a>=b) return gcd(a-b,b);
  else return gcd(a,b-a);
}

int32_t main(){
  ll a,b;
  cin>>a>>b;
  cout<<gcd(a,b);
  cout<<__gcd(a,b); //build-in function for c++
}
